# Streamlit Configuration Template
# Copy file này thành secrets.toml và điền thông tin thực của bạn

# API Keys (thay đổi giá trị dưới đây)
OPENROUTER_API_KEY = "your_openrouter_api_key_here"
TOGETHER_API_KEY = "your_together_api_key_here"

# Database
DATABASE_URL = "sqlite:///./quizzes.db"

# Application Settings  
DEBUG = true
HOST = "localhost"
PORT = 8000

# Model Configuration
DEFAULT_MODEL = "anthropic/claude-3-haiku"

# Vector Store
VECTOR_STORE_PATH = "./chroma_db"

# Security
SECRET_KEY = "your_secret_key_here"
CORS_ORIGINS = "http://localhost:3000,http://localhost:8501"

# ⚠️ QUAN TRỌNG:
# - File này chỉ là template
# - Tạo file secrets.toml với thông tin thật của bạn  
# - KHÔNG commit file secrets.toml vào Git
